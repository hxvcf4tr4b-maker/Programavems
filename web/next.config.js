/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: { unoptimized: true },
  // When served by Python backend, no base path needed
}

module.exports = nextConfig
